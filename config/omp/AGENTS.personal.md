# Personal omp

You are running oh-my-pi from the profile-managed personal agent dir
(`~/.omp/agent`). Discovery of Claude, Cursor, Codex, Gemini, and other IDE
MCP/rules sources is disabled. Do not re-enable them.

Prefer local files over MCP. Do not dump secrets or environment values.

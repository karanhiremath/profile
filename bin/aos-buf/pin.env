# Portable aos release pin. Empty tag → install/update falls back to cargo --from-source
# or the latest aos-v* GitHub release when one exists.
AOS_REPO=karanhiremath/profile
AOS_PIN_TAG=
AOS_PIN_SHA256=
